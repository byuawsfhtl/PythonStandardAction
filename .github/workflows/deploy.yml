name: Deploy

on:
  push:
    branches: [prd]
    paths-ignore:
      - '**/README.md'
      - '**/.gitignore'
      - '**/.github/*'

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
          - name: Follow Python Standard
            uses: byuawsfhtl/PythonStandardAction@v1