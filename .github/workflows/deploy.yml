name: Deploy

on:
  push:
    branches:
      - main

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
          - name: Follow Python Standard
            uses: byuawsfhtl/PythonStandardAction@v1.0.0